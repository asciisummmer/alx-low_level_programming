#include "main.h"
/**
 * _abs - check if character is lowercase
 * @val: value to be checked to be checked
 * Return:  val if number is pos or nul otherwise -val 
 */

int _abs(int val)
{
	if (val >= 0)
		return (val);
	return (-val);
}
